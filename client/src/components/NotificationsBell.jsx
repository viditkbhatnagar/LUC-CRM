// Stub for M3 — the live notifications panel + unread badge land in M5.
export default function NotificationsBell() {
  return (
    <button className="btn btn-ghost btn-sm" title="Notifications (M5)" aria-label="Notifications" disabled>
      🔔
    </button>
  );
}
